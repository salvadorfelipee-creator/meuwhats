{
  "name": "whatsapp-webhook",
  "version": "1.0.0",
  "description": "Webhook + painel simples para a API oficial do WhatsApp (Meta)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix painel-web && npm run build --prefix painel-web"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1108.0",
    "@aws-sdk/s3-request-presigner": "^3.1108.0",
    "@libsql/client": "^0.14.0",
    "busboy": "^1.6.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
